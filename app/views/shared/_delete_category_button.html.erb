<%= link_to '<i class="glyphicon glyphicon-remove"></i> Delete'.html_safe, category_path(category), method: :delete, data: {confirm: "Are you sure to delete " + category.name + "?"} , class: "btn btn-danger btn-sm" %>
